apiVersion: v1
kind: Service
metadata:
  name: governance-api-blue
  namespace: governance
spec:
  selector:
    app: governance-api
    color: blue
  ports:
    - port: 80
      targetPort: 8080
  type: ClusterIP